import { ApolloClient, InMemoryCache } from '@apollo/client';

//You can use this client to make requests to the graphql server here
//For more information, you should set up some defaultOptions to prevent some cache issues with the server

//setup the client to use the graphql server here

// export const client = new ApolloClient({
	
// });
